function showAge(age: number) {
  return age
    // console.log(age)
}

function greeting(name: string, function(age: number){
  console.log(age)
} {
    var age = 25
        // console.log('Hello ' + name);
        return "Hello" + name + callback(age)
  }
  
  async function processUserInput(callback: (name: string, callback: (age: number) => void) => void) {
    var name = "karl"
    return callback(name, showAge);
  }
  
  console.log('synchorized 1')
  processUserInput(greeting).then(v => console.log(v))
  
  console.log('synchorized 2')
